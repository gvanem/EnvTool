language: c

compiler:
  - gcc

before_script:
   - sudo add-apt-repository ppa:wine/wine-builds
   - sudo apt-get update

script:
  - env
  - make -C src -f Makefile.MinGW USE_64BIT=1 FORTIFY_SOURCE=0 TRAVIS_BUILD=1 depend all
